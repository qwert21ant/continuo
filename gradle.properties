org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# JDK 21 lives at a non-standard location on this machine and Gradle cannot auto-detect it.
org.gradle.java.installations.paths=C:\\SDK\\jdk-21.0.12

# Fabric Loom's plugin classpath itself requires a Java 21 runtime to load (independent of
# the Java 8 toolchain used to compile platform/core) - the Gradle daemon must run on JDK 21.
org.gradle.java.home=C:\\SDK\\jdk-21.0.12

group=dev.continuo
version=0.1.0-SNAPSHOT

minecraft_version=1.21.11
loader_version=0.19.3
fabric_version=0.141.6+1.21.11
loom_version=1.17.17

junit_version=5.11.4
